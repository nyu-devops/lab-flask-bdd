---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: petshop
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: petshop
            port:
              number: 8080
